# Gemini 開発ガイドライン

このドキュメントは、本プロジェクトにおける開発ガイドラインとコーディング規約を概説します。

## 1. 技術スタック

*   **フロントエンド:** React, TypeScript
*   **バックエンド:** Ruby on Rails (APIモード)
*   **データベース:** PostgreSQL
*   **AI:** Google Gemini API

## 2. コーディングスタイル

*   **フォーマット & リント:**
    *   **バックエンド (Ruby on Rails):** コミュニティで主流のスタイルガイドに基づき `RuboCop` を使用します。
    *   **フロントエンド (React/TypeScript):** コードフォーマットには `Prettier` を、リンティングには `ESLint` を使用します。
*   **命名規則:**
    *   `variables_and_functions`: `snake_case` (Ruby), `camelCase` (TypeScript)
    *   `Classes`: `PascalCase`
    *   `constants`: `UPPER_SNAKE_CASE`

## 3. ディレクトリ構成

プロジェクトは `frontend` と `backend` の2つの主要なディレクトリで構成されます。

*   `frontend/`: `create-react-app` によって作成された構成に基づく、React/TypeScriptアプリケーションを格納します。
*   `backend/`: `rails new --api` によって作成された構成に基づく、Ruby on Rails APIを格納します。

## 4. テスト

*   **バックエンド (Ruby on Rails):** ユニットテストと結合テストには `RSpec` を使用します。
*   **フロントエンド (React/TypeScript):** テストランナーには `Jest` を、コンポーネントテストには `React Testing Library` を使用します。

## 5. コミットメッセージ

*   **規約:** [Conventional Commits](https://www.conventionalcommits.org/) の仕様に従います。これにより、明確で自動化された変更履歴の維持が容易になります。
    *   一般的なプレフィックスには、`feat:`、`fix:`、`docs:`、`style:`、`refactor:`、`test:`、`chore:` が含まれます。

## 6. API設計

*   **スタイル:** RESTful

## 7. アーキテクチャ

### ディレクトリ構成と責務

本プロジェクトではクリーンアーキテクチャの原則を採用します。`app`ディレクトリは、関心を分離するために以下の思想で構成されます。

*   `app/controllers`: HTTPリクエストのエントリーポイントです。リクエストの解析、適切な`UseCase`の呼び出し、レスポンス（JSONなど）の返却に責務を持ちます。コントローラー自体にビジネスロジックは記述しません。
*   `app/models`: ActiveRecordのモデルであり、ドメインエンティティとして扱います。バリデーションやアソシエーションなど、そのエンティティに閉じたビジネスロジックをここに記述します。
*   `app/use_cases`: アプリケーションのユースケースを実装します。コントローラーから呼び出され、複数のモデルやリポジトリを操作して一連のビジネスフローを実現します。
*   `app/services`: 特定のモデルに属さない、ドメイン横断的なビジネスロジックを配置します（例: 外部APIとの連携サービス、複雑な計算処理など）。
*   `app/repositories`: データベース操作を抽象化する層です。`UseCase`はリポジトリを通じてデータの永続化や取得を行い、具体的なDB実装（ActiveRecordなど）から隔離されます。

## 8. Git運用ルール

AIによるコミットやプッシュの自動化を円滑にするため、以下のGit運用ルールを定めます。

### ブランチ戦略
*   **基本ブランチ**: `main`（本番）, `develop`（開発ベース）
*   **作業ブランチ**: `develop`から`feat/` `fix/`等のプレフィックスを付けて作成
*   **新規ブランチ作成時の注意**: 新しい作業ブランチを作成する際は、必ず`develop`ブランチを最新の状態（`git pull origin develop`）にしてからブランチを切ってください。

### プルリクエストの粒度
プルリクエストは、「レビュアーが短時間で理解できる、一つの完結した機能（または関心事）」を単位として作成します。モデル、コントローラー、テストコードなど、関連する変更をすべて含めることを原則とします。

### コミットの単位
「〇〇のロジック追加」のように、一つの関心事に集中した最小単位でコミットします。

### プルリクエストの自動作成
プルリクエストは、`gh`（GitHub CLI）コマンドを使用して自動で作成されます。本文には、定義された受け入れ条件が自動的に記載されます。

### Pushとプルリクエストのルール
1.  作業ブランチをPushする前、および**プルリクエストを作成する直前**には、必ずローカル環境で**全てのユニットテスト**を実行し、成功することを確認します。
2.  もしテストが一つでも失敗した場合は、プルリクエストを作成する前に必ず修正します。
3.  ローカルでのテストをすべてクリアした後、`develop`ブランチにマージするためのプルリクエストを作成します。
4.  プルリクエストの概要欄には、開発前に定義した「受け入れ条件」をチェックリスト形式で必ず記載します。
5.  **[重要]** プルリクエストがマージされるためには、CI/CDによる自動テストがすべて成功していることを必須条件とします。

## 9. 開発ワークフロー

品質の高い開発サイクルを維持するため、以下のワークフローを定めます。

### 1. 受け入れ条件の定義
コーディング開始前に、対話を通じてタスクの**受け入れ条件（完了の定義）**を明確にします。

### 2. 開発環境の準備
コーディングに着手する前に、以下の手順で開発環境がクリーンな状態であることを確認します。

1.  **差分ファイルの確認**: `git status`を実行し、コミットされていない変更が作業ディレクトリにないことを確認します。もし差分が存在する場合は、ユーザーに確認を求め、作業を一時停止します。
2.  **developブランチへの移動**: `git checkout develop`を実行し、`develop`ブランチに切り替えます。
3.  **最新化**: `git pull origin develop`を実行し、ローカルの`develop`ブランチを最新の状態に更新します。

### 3. 実装と仕様書の更新
1.  受け入れ条件と開発環境の準備が整い次第、コーディングに着手します。
2.  実装完了後、コードの変更と**同じプルリクエスト内**で、`docs/spec`ディレクトリにある関連仕様書を更新します。

### 4. タスク管理
開発タスクは、`tasks/`ディレクトリ以下で管理されます。

*   **`tasks/overview.md`**: プロジェクト全体の開発タスク（PR単位）の進捗を一覧で管理するマスターファイルです。常に更新され、最新のタスク状況を反映します。**各タスクの実装が完了し、プルリクエストをプッシュする直前に、このファイルを更新します。**
*   **`tasks/details/PR[番号]-[簡潔な説明].md`**: 各タスクの詳細情報（受け入れ条件、関連PR、メモなど）を記載する個別ファイルです。`tasks/overview.md`からリンクされます。
